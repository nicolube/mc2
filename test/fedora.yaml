---
base: fedora:rawhide
install:
  - echo
  - gcc
  - curl
  - openssl-devel
---
#!/bin/bash

# Install Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
. "$HOME/.cargo/env"

# Install MC2
cargo install mc2
